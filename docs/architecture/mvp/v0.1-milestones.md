# MVP v0.1 Milestones

```mermaid
gantt
  title Suggested delivery order
  dateFormat YYYY-MM
  section Foundation
  ADRs and docs           :done, 2026-07, 1w
  Kithara scaffold        :2026-07, 2w
  section Modules
  local auth + JWT        :2026-08, 2w
  youtube-module          :2026-08, 3w
  section Core
  Neck FIFO session       :2026-08, 3w
  Stream Server ICY       :2026-09, 2w
  section UI
  Plume MVP               :2026-09, 3w
  section Ops
  Compose + OTel wiring   :2026-09, 1w
```

## Ordered milestones

1. **Architecture docs** (this repository) — ADRs, interfaces, deployment shape
2. **Kithara skeleton** — feature-first API layout, auth orchestrator, module registry, user + binding tables
3. **Local password + JWT sessions** — discovery, authenticate/refresh, bootstrap admin
4. **Source module protocol** — gRPC + FIFO PCM proof with YouTube (`StartTrack` / `StopTrack`)
5. **Neck refactor** — alive-on-create, session FIFO, silence feeder, hosted FFmpeg supervisor
6. **Stream Server** — ICY-over-HTTP `/stream/{slug}`
7. **Plume** — `/`, `/player/{slug}`, discovery-driven login (optional client)
8. **Compose bundle** — edge + 4 apps; wire to **external** OTel collector; join secrets for modules

**Related:** [v0.1-scope.md](v0.1-scope.md)

**Read next:** [../spike/prototype-neck-ffmpeg.md](../spike/prototype-neck-ffmpeg.md)
